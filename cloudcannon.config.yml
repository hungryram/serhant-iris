collections_config:
  data:
    path: "data"
    label: "Profile and Settings"
  pages:
    path: "content/pages"
    output: true
    _enabled_editors:
      - visual
      - content
paths:
  uploads: /images