collections_config:
  data:
    path: "data"
    label: "Profile and Settings"
  pages:
    path: "content/pages"
    output: true
    url: "/[slug]"
    _enabled_editors:
      - visual
      - content
paths:
  uploads: static/images
_inputs:
  body:
    type: markdown
  logo:
    type: image
  photo:
    type: image